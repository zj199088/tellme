# 健身应用自定义计划功能 - 实现计划

## [x] 任务1：修改后端PlanController创建自定义计划时同时创建模板（包括template_days和template_exercises）
- **优先级**：P0
- **依赖**：无
- **描述**：
  - 修改`createCustomPlan`方法，使其在创建fitness_plans的同时创建templates
  - 确保template的创建者设置为当前用户
  - 确保template的isPublic字段设置为0（私有）
  - 建立计划与模板的关联关系
- **验收标准**：AC-1
- **测试要求**：
  - `programmatic` TR-1.1：调用创建自定义计划接口后，验证fitness_plans和templates表都有相应记录
  - `programmatic` TR-1.2：验证template的createdBy字段正确设置为当前用户
- **注意**：需要处理计划和模板的关联关系，确保数据一致性

## [x] 任务2：实现模板权限控制接口
- **优先级**：P0
- **依赖**：任务1
- **描述**：
  - 创建获取用户模板的接口，只返回用户自己的模板和公开的模板
  - 实现模板的权限检查逻辑
- **验收标准**：AC-2
- **测试要求**：
  - `programmatic` TR-2.1：用户A只能看到自己的模板和公开的模板
  - `programmatic` TR-2.2：用户B无法看到用户A的私有模板
- **注意**：确保权限控制的安全性，防止越权访问

## [x] 任务3：实现模板分享功能
- **优先级**：P0
- **依赖**：任务2
- **描述**：
  - 完善`sharePlan`接口，实现将模板设置为公开
  - 确保分享的模板显示创建用户和时间信息
- **验收标准**：AC-3
- **测试要求**：
  - `programmatic` TR-3.1：用户A分享模板后，用户B可以看到该模板
  - `programmatic` TR-3.2：分享的模板显示创建用户和时间信息
- **注意**：考虑分享的安全性，确保只有模板创建者可以分享

## [x] 任务4：确保自定义计划训练跟踪逻辑
- **优先级**：P1
- **依赖**：任务1
- **描述**：
  - 确保自定义计划的训练跟踪逻辑与模板生成计划相同
  - 验证训练记录的创建和查询功能
- **验收标准**：AC-4
- **测试要求**：
  - `programmatic` TR-4.1：自定义计划的训练跟踪功能正常工作
  - `programmatic` TR-4.2：训练记录正确关联到自定义计划
- **注意**：复用现有的训练跟踪逻辑，确保一致性

## [x] 任务5：修改前端自定义计划创建页面
- **优先级**：P1
- **依赖**：任务1
- **描述**：
  - 确保前端传递正确的数据格式到后端
  - 处理创建成功后的跳转和提示
- **验收标准**：AC-1
- **测试要求**：
  - `human-judgement` TR-5.1：前端创建自定义计划界面正常
  - `programmatic` TR-5.2：前端正确传递数据到后端
- **注意**：保持前端界面的美观性和用户体验

## [x] 任务6：测试所有功能
- **优先级**：P2
- **依赖**：所有任务
- **描述**：
  - 测试创建自定义计划功能
  - 测试模板权限控制功能
  - 测试模板分享功能
  - 测试自定义计划训练跟踪功能
- **验收标准**：所有AC
- **测试要求**：
  - `programmatic` TR-6.1：所有功能正常工作
  - `human-judgement` TR-6.2：用户体验良好
- **注意**：确保所有边界情况都被测试到