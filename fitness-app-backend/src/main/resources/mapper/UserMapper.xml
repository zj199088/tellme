<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitness.app.mapper.UserMapper">

    <select id="selectByOpenId" parameterType="java.lang.String" resultType="com.fitness.app.entity.User">
        SELECT id, open_id as openId, nickname, avatar_url as avatarUrl, gender, country, province, city, role, username, password, fitness_goals as fitnessGoals, fitness_level as fitnessLevel, height, weight, is_deleted as isDeleted, created_at as createdAt, updated_at as updatedAt FROM users WHERE open_id = #{openId} AND is_deleted = 0
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultType="com.fitness.app.entity.User">
        SELECT id, open_id as openId, nickname, avatar_url as avatarUrl, gender, country, province, city, role, username, password, fitness_goals as fitnessGoals, fitness_level as fitnessLevel, height, weight, is_deleted as isDeleted, created_at as createdAt, updated_at as updatedAt FROM users WHERE username = #{username} AND is_deleted = 0
    </select>

</mapper>